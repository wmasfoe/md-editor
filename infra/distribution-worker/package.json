{
  "name": "@md-editor/distribution-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker edge gateway for Inkpoint and multi-app installer distribution",
  "type": "module",
  "scripts": {
    "build": "node --experimental-strip-types scripts/build-static-portal.ts",
    "predev": "node --experimental-strip-types scripts/build-static-portal.ts",
    "predeploy": "node --experimental-strip-types scripts/build-static-portal.ts",
    "dev": "npx wrangler dev",
    "deploy": "pnpm build && npx wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
